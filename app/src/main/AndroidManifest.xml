<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Include required permissions for Google Mobile Ads to run. -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="com.google.android.gms.permission.AD_ID" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />  <!--For speech to text-->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM"/>
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" android:maxSdkVersion="31" />
    <!--
    about setting USE_EXACT_ALARM" android:maxSdkVersion="31":
    - In Android 12, Google introduced the USE_EXACT_ALARM permission
    - In Android 13, they changed the permission model to use SCHEDULE_EXACT_ALARM instead
    - The maxSdkVersion="31" ensures you're not requesting a deprecated permission on newer Android versions
    without  maxSdkVersion:
    - On Android 13+, the system would show a warning that you're requesting a deprecated permission
    - Your app might be rejected from the Play Store for requesting unnecessary permissions
    -->


    <application
        android:name=".FreddyFridgeApp"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:hardwareAccelerated="true"
        tools:targetApi="31">



        <activity
            android:name=".feat_starting.presentation.ui.intromain.MainActivity"
            android:exported="true"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Used for notifications -->
        <service
            android:name=".core.reminder.FoodReminderIntentService"
            android:exported="false" />
        <receiver
            android:name=".core.reminder.withalarmmanager.AlarmReceiver"
            android:enabled="true"
            android:exported="true">
        </receiver>
    </application>

</manifest>